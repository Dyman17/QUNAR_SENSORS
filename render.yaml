services:
  - type: web
    name: esp-receiver-site
    env: python
    rootDir: esp-receiver-site
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
